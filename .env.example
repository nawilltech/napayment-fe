# Copy to apps/web/.env.local for local development.

# The napayment Java backend (see ../napayment). Server-side only - never
# exposed to the browser (doc F2 ADR-FE-2, the BFF pattern). Must be reachable
# from wherever this Next.js server runs.
NAWILL_API_BASE_URL=http://localhost:8080
